import React from 'react';

import {Container} from './styles';
import {Markdown} from 'components';

const FeedHome = () => {
  return (
    <Container>
      <Markdown text="Tela do feed" />
    </Container>
  );
};

export default FeedHome;
